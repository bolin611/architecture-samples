package com.example.android.architecture.blueprints.todoapp.study.serialiable

class GroupConstants {
    class Serialiable{
        companion object {
            const val sendPath = "/test/send"
            const val acceptPath = "/test/accept"
            const val param = "param"
        }
    }
}